{
	"name": "remote-pc-controler",
	"private": "true",
	"version": "1.0.0",
	"description": "monorepo of mobile and pc agent code.",
	"main": "index.js",
	"workspaces": [
		"app/*",
		"package/*"
	],
	"scripts": {
		"mobile": "turbo run start --filter=mobile",
		"build:mobile": "turbo run build --filter=mobile",
		"cmake:agent":"cd agent/build && cmake .. .",
		"debug:agent": "cmake --build agent/build",
		"build:agent": "cmake --build agent/build --config Release",
		"cmake:debug:agent": "npm run cmake:agent && npm run debug:agent",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"type": "commonjs",
	"devDependencies": {
		"turbo": "^2.8.16"
	}
}
